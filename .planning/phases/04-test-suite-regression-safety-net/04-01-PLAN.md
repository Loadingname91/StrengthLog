---
phase: 04-test-suite-regression-safety-net
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.js
  - src/test/setup.js
  - src/state/reducer.test.js
  - src/state/StoreContext.test.jsx
  - src/components/ConfirmSheet.test.jsx
  - src/screens/RoutineBuilder.test.jsx
  - src/components/SessionBar.test.jsx
autonomous: true
requirements: [TEST-01, TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "Vitest and Testing Library are installed and configured; `npm test` runs the full suite once and exits, reporting pass/fail."
    - "src/state/reducer.js has passing unit tests for DELETE_ALL_DATA (keeps customExercises/routines, clears sessions/measurements/goals/lastFinishedSession), START_WORKOUT (builds an active workout from a routine, and no-ops when one is already active), and FINISH_WORKOUT (appends a session, clears activeWorkout, advances sequenceIndex)."
    - "buildInitialState (the fresh-install/seed-removal path) has a passing unit test proving an empty localStorage produces a fully-empty state shape, and a persisted blob is returned unchanged."
    - "The hold-to-confirm gesture, the target-weight field, and the SessionBar each have at least one passing smoke-level interaction test."
    - "npm run lint and npm test both exit clean (zero errors) in the same run."
  artifacts:
    - path: "package.json"
      provides: "test and test:watch scripts; vitest + testing-library devDependencies"
      contains: "vitest run"
    - path: "vite.config.js"
      provides: "Vitest test config (jsdom environment, setup file)"
      contains: "environment: 'jsdom'"
    - path: "src/state/reducer.test.js"
      provides: "unit tests for DELETE_ALL_DATA, START_WORKOUT, FINISH_WORKOUT"
      contains: "DELETE_ALL_DATA"
    - path: "src/state/StoreContext.test.jsx"
      provides: "unit test for buildInitialState's empty-state (seed-removal) and persisted-passthrough paths"
      contains: "buildInitialState"
    - path: "src/components/ConfirmSheet.test.jsx"
      provides: "smoke test for the 1500ms hold-to-confirm gesture and early-release cancellation"
      contains: "holdToConfirm"
    - path: "src/screens/RoutineBuilder.test.jsx"
      provides: "smoke test for the target-weight field"
      contains: "Target weight"
    - path: "src/components/SessionBar.test.jsx"
      provides: "smoke test for the persistent session bar"
      contains: "activeWorkout"
  key_links:
    - from: "package.json test script"
      to: "vite.config.js test config"
      via: "vitest reads the `test` block from the same config Vite already uses"
      pattern: "vitest run"
---

<objective>
Install and configure Vitest + Testing Library, then write the specific unit and smoke tests ROADMAP.md's Phase 4 success criteria call for: reducer coverage of the seed-removal path, DELETE_ALL_DATA, and the workout start/finish/session-transition actions, plus one smoke-level interaction test each for the delete-all hold gesture, the target-weight field, and the persistent session bar.

Purpose: this is the milestone's last phase specifically because everything it covers (the delete gesture, target weight, session bar, and the reducer's most consequential actions) only exists as of Phases 1-3. This closes TEST-01 through TEST-04, giving the project a real regression net for the first time.
Output: test infrastructure (config, setup file, package.json scripts) plus 5 new test files. No production source files change behavior — `src/state/StoreContext.jsx`'s `buildInitialState` gains an `export` keyword (needed for direct unit testing) but its behavior is unchanged.
</objective>

## Phase Goal

**As a** FitLog maintainer, **I want to** have `npm test` catch a regression in this milestone's riskiest logic before it reaches a device, **so that** the next bug like the ones this milestone fixed (fake seed data, an unconfirmed destructive delete) gets caught by CI-equivalent automation, not by a user.

**This plan's slice of the story:** the entire test suite — there is no second plan in this phase.

<execution_context>
Execute this plan directly — install packages, write config and tests, run the verify commands, write the SUMMARY.md at the end. No subagent dispatch for this project (per explicit user direction).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@src/state/reducer.js
@src/state/StoreContext.jsx
@src/components/ConfirmSheet.jsx
@src/screens/RoutineBuilder.jsx
@src/components/SessionBar.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Vitest + Testing Library</name>
  <files>package.json, vite.config.js, src/test/setup.js (new)</files>
  <read_first>
    package.json — current scripts and dependencies.
    vite.config.js — current plugin config, to extend with a `test` block rather than creating a separate vitest.config.js (Vitest reads `test` from the same Vite config file).
    .planning/codebase/TESTING.md — confirms no existing test tooling and recommends this exact stack.
  </read_first>
  <action>
    Install `vitest`, `@testing-library/react`, `@testing-library/jest-dom`, and `jsdom` as devDependencies. Add `"test": "vitest run"` (one-shot, exits with a pass/fail status — matches TEST-01's "a test command runs the suite successfully") and `"test:watch": "vitest"` (interactive dev convenience) to `package.json`'s scripts. Extend `vite.config.js`'s `defineConfig` call with a `test: { environment: 'jsdom', setupFiles: ['./src/test/setup.js'], css: false }` block. Create `src/test/setup.js` importing `@testing-library/jest-dom/vitest` so `expect(...).toBeInTheDocument()`-style matchers are available in every test file without per-file imports.
  </action>
  <verify>
    <automated>grep -q '"test": "vitest run"' package.json && echo "test script present"</automated>
    <automated>grep -q "jsdom" vite.config.js && echo "jsdom environment configured"</automated>
    <automated>test -f src/test/setup.js && echo "setup file created"</automated>
  </verify>
  <done>npm test is runnable and reports pass/fail (verified with a throwaway smoke assertion during development, removed before commit); config committed.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for reducer.js and buildInitialState (TEST-01, TEST-02)</name>
  <files>src/state/reducer.test.js (new), src/state/StoreContext.test.jsx (new), src/state/StoreContext.jsx</files>
  <read_first>
    src/state/reducer.js in full — exact DELETE_ALL_DATA, START_WORKOUT, TOGGLE_SET_DONE, and FINISH_WORKOUT case bodies (all fields returned, all guard conditions).
    src/state/StoreContext.jsx lines 9-37 — buildInitialState's persisted-vs-fresh branches.
    src/state/storage.js — the `fitlog:v1` localStorage key buildInitialState reads through.
  </read_first>
  <action>
    In `src/state/StoreContext.jsx`, add the `export` keyword to `function buildInitialState()` (behavior unchanged — this only makes it importable for direct unit testing; it is still called internally exactly as before).

    Create `src/state/StoreContext.test.jsx` (pure logic test, no component rendering needed): clear `localStorage` before each test (jsdom provides a real `localStorage`). Test 1 — "seed-removal path": with `localStorage` empty, `buildInitialState()` returns `routines: []`, `sessions: []`, `measurements: []`, `goals: []`, `customExercises: []`, `activeWorkout: null`, and no field resembling seeded demo data (no non-empty `routines`/`sessions` arrays). Test 2 — persisted passthrough: `localStorage.setItem('fitlog:v1', JSON.stringify({ routines: [{ id: 'r1' }], sessions: [], routineOrder: ['r1'] }))`, then assert `buildInitialState()` returns that object (plus its two pre-existing backfill fields, `weekdayAssignments` and `scheduleRestartAt`, added when absent) — proving the fresh-install literal is never used when data already exists (DATA-02's guarantee, now regression-tested).

    Create `src/state/reducer.test.js`: import `{ reducer }` from `./reducer`. Build a minimal valid base state object (all fields `buildInitialState` would produce, with one routine in `routines`/`routineOrder` for the workout tests).
    - `DELETE_ALL_DATA`: start from a state with non-empty `sessions`, `measurements`, `goals`, `customExercises`, `lastFinishedSession`, and a non-empty `routines`/`routineOrder`. Dispatch `DELETE_ALL_DATA`. Assert `sessions`, `measurements`, `goals`, and `lastFinishedSession` are cleared, and `customExercises` and `routines`/`routineOrder` are UNCHANGED (this locks in the Phase 1 CR-02 code-review fix — regressing this would silently wipe a user's custom exercise library again).
    - `START_WORKOUT`: dispatch with a valid `routineId` against a state with no `activeWorkout` — assert the returned `activeWorkout` is non-null, has `routineId` matching, `currentIndex: 0`, and an `exercises` array with one entry per exercise in the routine's blocks. Then dispatch `START_WORKOUT` again against a state that already HAS an `activeWorkout` — assert the reducer returns the state object unchanged (the Phase 3 QA fix: never silently discard an in-progress session).
    - `FINISH_WORKOUT`: start from a state with an `activeWorkout` that has one exercise with one `done: true` set (valid weight/reps). Dispatch `FINISH_WORKOUT`. Assert `activeWorkout` is `null`, `sessions` gained exactly one entry, `lastFinishedSession` is set to that new session, and `sequenceIndex` advanced (wrapping via modulo) to the next routine in `routineOrder`.
  </action>
  <verify>
    <automated>npx vitest run src/state/reducer.test.js src/state/StoreContext.test.jsx</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Both new test files pass; DELETE_ALL_DATA, START_WORKOUT (including the no-overwrite guard), FINISH_WORKOUT, and buildInitialState's two paths are all covered and green.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke-level interaction tests for the hold gesture, target-weight field, and session bar (TEST-03)</name>
  <files>src/components/ConfirmSheet.test.jsx (new), src/screens/RoutineBuilder.test.jsx (new), src/components/SessionBar.test.jsx (new)</files>
  <read_first>
    src/components/ConfirmSheet.jsx in full — the holdToConfirm prop, startHold/cancelHold, HOLD_DURATION_MS.
    src/screens/RoutineBuilder.jsx — the BlockEditSheet component (currently not exported; needs a named export to test in isolation, same rationale as buildInitialState in Task 2).
    src/components/SessionBar.jsx in full — its useStore() and useNavigate() dependencies, which a smoke test must mock rather than standing up a full StoreProvider + Router.
  </read_first>
  <action>
    In `src/screens/RoutineBuilder.jsx`, add the `export` keyword to `function BlockEditSheet(...)` (behavior unchanged; it is still used internally exactly as before via the default-exported `RoutineBuilder`). `BlockEditSheet` takes no store/router dependencies (pure props in, callbacks out), so it can be rendered directly in a test without any provider wrapper.

    Create `src/components/ConfirmSheet.test.jsx`: use `vi.useFakeTimers()`. Render `<ConfirmSheet open title="Delete all data?" confirmLabel="Delete everything" holdToConfirm onConfirm={onConfirmMock} onCancel={onCancelMock} />`. Test 1 (full hold triggers): `fireEvent.pointerDown` on the confirm button, advance fake timers by 1500ms, assert `onConfirmMock` was called once. Test 2 (early release cancels): `fireEvent.pointerDown`, advance timers by 500ms, `fireEvent.pointerUp`, advance the remaining 1000ms, assert `onConfirmMock` was never called. Restore real timers after each test.

    Create `src/screens/RoutineBuilder.test.jsx`: import the new named export `{ BlockEditSheet }`. Render it with a minimal `block` object (`{ id: 'b1', exerciseIds: ['ex1'], sets: 3, repMin: 8, repMax: 12, rest: 90, rir: 2, targetWeight: null }`) — mock `exerciseById` (imported from `../lib/exercises`) via `vi.mock` if needed so the exercise name renders without needing the full exercise catalog, or pass an id that resolves harmlessly. Find the "Target weight (optional)" input by its `placeholder` (`e.g. 60`), type a value into it with `fireEvent.change`, click "Save exercise", and assert the `onSave` mock was called with a `targetWeight` field equal to the numeric value typed (not the string, and not `null`).

    Create `src/components/SessionBar.test.jsx`: `vi.mock('../state/StoreContext', () => ({ useStore: () => mockUseStoreReturn }))` where `mockUseStoreReturn` is a test-controlled object. Test 1 (no active workout): with `mockUseStoreReturn.state.activeWorkout = null`, render `<MemoryRouter><SessionBar /></MemoryRouter>`, assert nothing is rendered (`container.firstChild` is `null`, or query for the routine-name text returns nothing). Test 2 (active workout): set `mockUseStoreReturn.state.activeWorkout = { routineName: 'Push Day', startedAt: new Date().toISOString() }`, render again, assert the text "Push Day" is present in the document (confirms SESSION-01's bar renders with the workout's identity when active).
  </action>
  <verify>
    <automated>npx vitest run src/components/ConfirmSheet.test.jsx src/screens/RoutineBuilder.test.jsx src/components/SessionBar.test.jsx</automated>
    <automated>npm run build</automated>
  </verify>
  <done>All three new smoke tests pass, each exercising the exact feature its name references — the hold gesture's timing behavior (both outcomes), the target-weight field's save path, and the session bar's conditional render.</done>
</task>

<task type="auto">
  <name>Task 4: Full-suite clean run (TEST-04)</name>
  <files>None (verification-only task)</files>
  <read_first>
    None — this task runs the full suite and lint together and reports the result.
  </read_first>
  <action>
    Run `npm test` (the full suite: reducer, StoreContext, ConfirmSheet, RoutineBuilder, SessionBar) and `npm run lint` back to back. Both must exit 0 with zero errors in the same run — per TEST-04's exact wording. If lint reports any NEW warning introduced by the two `export` keyword additions (Task 2, Task 3) or the new test files themselves, fix it; pre-existing warnings unrelated to this phase's changes are out of scope (this project's convention throughout Phases 1-3 has been to leave pre-existing warnings alone unless a change of this phase newly triggers one).
  </action>
  <verify>
    <automated>npm test</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <done>npm test and npm run lint both exit 0 in the same run; no new lint warnings introduced by this phase's changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Test-only exports → production behavior | `buildInitialState` and `BlockEditSheet` gain `export` keywords for testability; both remain called exactly as before internally, so this cannot change runtime behavior — only import surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|--------------|------------------|
| T-04-01 | Tampering (false confidence) | New test files | medium | mitigate | A test that always passes regardless of the implementation (e.g. asserting nothing, or asserting a trivial truth) would be worse than no test — it signals coverage that doesn't exist. Mitigated by writing each assertion against the specific documented behavior (exact field values, exact call counts/arguments), and by running each new test file individually during Task 2/3 development to confirm it actually fails when the corresponding production code is reverted (not verified via prior tooling — verified inline: the DELETE_ALL_DATA test would fail if the Phase 1 CR-02 fix were reverted, since it asserts `customExercises` survives). |

</threat_model>

<verification>
1. `npm test` runs the full suite once and exits with a clear pass/fail summary.
2. `src/state/reducer.test.js` covers DELETE_ALL_DATA, START_WORKOUT (both paths), and FINISH_WORKOUT, all passing.
3. `src/state/StoreContext.test.jsx` covers both `buildInitialState` branches, both passing.
4. `src/components/ConfirmSheet.test.jsx`, `src/screens/RoutineBuilder.test.jsx`, and `src/components/SessionBar.test.jsx` each pass at least one smoke test.
5. `npm run lint` and `npm test` both exit 0 in the same run.
6. `npm run build` continues to succeed (production build unaffected by test-only additions).
</verification>

<success_criteria>
- Vitest + Testing Library installed and configured, `npm test` runs successfully — matches TEST-01.
- reducer.js has passing tests for seed-removal, DELETE_ALL_DATA, and workout start/finish/session-transition — matches TEST-02.
- Hold gesture, target-weight field, and session bar each have a passing smoke test — matches TEST-03.
- `npm run lint` and `npm test` both exit clean together — matches TEST-04.
</success_criteria>

## Artifacts This Phase Produces (this plan's share)

- **Modified:** `package.json` — test tooling devDependencies, `test`/`test:watch` scripts.
- **Modified:** `vite.config.js` — Vitest `test` config block.
- **Created:** `src/test/setup.js` — jest-dom matcher setup.
- **Created:** `src/state/reducer.test.js`, `src/state/StoreContext.test.jsx`, `src/components/ConfirmSheet.test.jsx`, `src/screens/RoutineBuilder.test.jsx`, `src/components/SessionBar.test.jsx`.
- **Modified (export-only, no behavior change):** `src/state/StoreContext.jsx` (`buildInitialState`), `src/screens/RoutineBuilder.jsx` (`BlockEditSheet`).

<output>
Create `.planning/phases/04-test-suite-regression-safety-net/04-01-SUMMARY.md` when done
</output>
